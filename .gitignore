node_modules/
package-lock.json
.env
__pycache__/
*.pyc
.venv/
venv/
screenshot.png

# The learning store and everything scraped into it.
data/

# Shader previews, rendered on demand by the headless renderer.
sidequest/docs/preview-*.png

# Agent tooling, installed with `npx skills add`. Not part of the project.
.agents/

# Browser-probe output, regenerated by `npm run probe`.
sidequest/docs/probe-*.png

sidequest/docs/probe-logo.png
